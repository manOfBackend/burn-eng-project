{
  "private": true,
  "scripts": {
    "build": "dotenv -- turbo run build",
    "dev": "dotenv -- turbo run dev",
    "db:generate": "dotenv -- turbo run db:generate --filter=@sayvoca/database",
    "db:push": "dotenv -- turbo run db:push --filter=@sayvoca/database",
    "db:studio": "dotenv -- turbo run db:studio --filter=@sayvoca/database",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@sayvoca/config": "workspace:*",
    "@turbo/gen": "^1.9.7",
    "dotenv-cli": "^7.2.1",
    "eslint": "^7.32.0",
    "prettier": "^2.5.1",
    "turbo": "latest",
    "typescript": "^5.1.3"
  },
  "packageManager": "pnpm@7.15.0",
  "name": "sayvoca-with-gpt",
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
